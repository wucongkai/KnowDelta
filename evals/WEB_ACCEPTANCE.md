# Web 工作区验收

日期：2026-09-15。环境：macOS Apple Silicon；本机 Python 3.12 / Node 24；Docker Linux ARM64。

## 已通过

- Python：40 个测试通过，包含媒体处理、字幕、缓存、模型协议、API、任务去重和消息派发失败；另有 2 条上游测试依赖弃用提示。
- 前端：3 个 Vitest 测试通过；TypeScript 类型检查、Vite 生产构建、Prettier 检查通过。
- Ruff 检查、格式检查、uv 锁文件检查、Git whitespace 检查通过。
- PostgreSQL 真实数据库迁移和已有 CLI 笔记导入成功。
- 网页提交真实 BV1p5qhYsE4f 视频，经过 API → 数据库 → Redis → Celery → Python 管线完成；此次复用了之前获取/转写的素材缓存。
  - 任务 ID：`97e83483-28f7-4ecd-9122-537e390a8b8c`
  - 标题：Mysql是什么？架构是怎么样的？
  - 时长约 10:14，4 个章节，7 张去重配图，241 条转写。
- 真实 HTTP 冒烟测试：合成 12 秒视频及 SRT 字幕，通过上传接口提交，验证生成成功、正文、截图、视频 `206 Range` 和 ZIP 完整性。
- 失败链路：损坏的视频上传后明确失败，可重试并取消；不会停留在永久运行状态。
- 回归测试：相同视频字节以不同文件名上传，继续复用媒体缓存，但正文标题使用当前文件名。
- 浏览器交互：导入表单与模型未配置状态、任务完成切换、笔记搜索、列表/网格、图片放大、原始转写、段落回跳到视频 30 秒、手机导航均通过。
- 1440×1000 桌面与 390×844 手机断点检查通过；手机文档没有横向溢出，收起的导航不占可视区域。
- 浏览器控制台未发现错误。
- 完整前后端镜像构建成功，容器化 PostgreSQL/Redis、迁移、API、Worker、Nginx 已实际启动，API 健康检查通过。
- 同一 HTTP 冒烟脚本通过 Nginx 的 8080 入口再次通过，证明独立容器间的上传、共享产物和反向代理链路可用。

本机自动化验收创建的合成视频任务记录已清理，真实 B 站任务和原始导入笔记保留。`backend/smoke_web.py` 可重复执行；每次会在所选工作区创建两条验收任务。

## 验证边界

真实视频使用原文整理模式。现有 ASR 对专业名词仍有识别错误；本次没有把原文整理结果当作模型精读质量验收。尚未提供模型服务配置，文字/视觉模型仅做已有 HTTP mock 协议测试，未验证真实供应商输出质量。

没有执行公网部署、多人权限或高并发负载验收。当前实现用于本机单人工作区。
